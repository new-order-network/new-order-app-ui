import { ModalOverlay as ChakraModalOverlay } from '@chakra-ui/react'

const ModalOverlay = () => {
  return <ChakraModalOverlay bg="blackAlpha.300" backdropFilter="blur(5px) " />
}

export default ModalOverlay
