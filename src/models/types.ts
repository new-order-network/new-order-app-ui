export enum API_METHOD {
  GET = 'GET',
  POST = 'POST',
}

export enum NEWS_TYPES {
  ALL = 'all',
  UPDATES = 'updates',
  VIDEO = 'video',
  ARTICLE = 'article',
}

export enum ACTIVE_VAULT_DEPOSIT_ACTIONS {
  WITHDRAW = 'WITHDRAW',
  CLAIM = 'CLAIM',
  WITHDRAW_AND_CLAIM = 'WITHDRAW_AND_CLAIM',
}

export enum LOCK_ACTIONS {
  APPROVE = 'APPROVE',
  LOCK = 'LOCK',
}
